# Plan: close the ADR 0069-0071 review findings

- Make the 0069 probe cover a tail-positioned `moov` box and require a regression check.
- Close 0070's rollout gap, give WebP intake an executable owner, and name Android's profile constant correctly.
- Make 0071's timeout, rendition generation, and admission contracts executable and internally consistent.
- Quarantine every condemned Asset without interrupting existing snapshot playback, remove airing as
  a queue branch, and assign the conversion-job schema to 0071's first migration.
- Make the 0070 backfill rollback target the same literal Asset ids as the forward update.
- Re-read the affected documents and grep for the superseded claims.
