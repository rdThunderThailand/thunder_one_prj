import { AssetAdminDashboardPage } from "@/features/asset-intelligence/assets";

// Asset/IT Manager ("Asset Admin") — landing dashboard, matching the
// reference mockup (Nie, 2026-08-26). The asset list this route used to
// render moved to ./all — see AllAssetsPage.
export default function AssetIntelligenceAssetsPage() {
  return <AssetAdminDashboardPage />;
}
