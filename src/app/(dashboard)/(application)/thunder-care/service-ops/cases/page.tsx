import { CasesInboxPage } from "@/features/thunder-care/service-ops";

// Thunder Care — "เคสจากลูกค้า" (inbox เต็มของทุกช่องทาง).
export default function CasesInboxRoute() {
  return <CasesInboxPage />;
}
