import { CaseTrackingPage } from "@/features/thunder-care/service-ops";

// Thunder Care — "ติดตามเคส" (ความคืบหน้าของเคสที่ผ่าน Triage ไปแล้ว).
export default function CaseTrackingRoute() {
  return <CaseTrackingPage />;
}
