import { ChangesPage } from "@/features/people/changes";

// HR Manager — change requests roster + approve/reject detail panel ("การเปลี่ยนแปลง").
export default function PeopleChangesPage() {
  return <ChangesPage />;
}
