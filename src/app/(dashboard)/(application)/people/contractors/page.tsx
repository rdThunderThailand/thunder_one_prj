import { ContractorsPage } from "@/features/people/contractors";

// HR Manager — the standalone Contractor roster page. Fully mock, no
// server fetch needed — see people/contractors/mock-data.ts's header
// comment for why.
export default function PeopleContractorsPage() {
  return <ContractorsPage />;
}
