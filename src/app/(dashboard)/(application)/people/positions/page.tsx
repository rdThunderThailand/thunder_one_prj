import { PositionsPage } from "@/features/people/positions";

// HR Manager — "ตำแหน่งงาน" (Positions) page. Fully mock, no server fetch
// needed — see people/positions/mock-data.ts's header comment for why.
export default function PeoplePositionsPage() {
  return <PositionsPage />;
}
